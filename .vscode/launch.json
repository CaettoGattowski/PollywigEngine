{
    "version" : "0.0.1",
    "configurations": [
        {

            "name" : "Run PollywigEngine",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/PollywigEngine",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false,
            "terminal": "external",
            "env": {
                "GDK_BACKEND": "x11",
                "SDL_VIDEODRIVER": "x11",
                "LSAN_OPTIONS": "detect_leaks=0"
            }
        }
    ]
}